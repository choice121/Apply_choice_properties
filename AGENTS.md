# ⛔ STOP — AI AGENT HARD BLOCK

This file is the primary AI behavior contract for this repository.
It is recognized by OpenAI Codex, Claude, Cursor, Copilot, and most agent frameworks.
**Every rule below is non-negotiable and applies in every environment.**

---

## WHAT THIS PROJECT IS

A static website: plain HTML, CSS, and vanilla JavaScript.
Backend: Google Apps Script only (deployed separately to Google).
Hosting: Cloudflare Pages only.
Editing: Done in any code editor (Replit, VS Code, Cursor, etc.) — editor use only.

## WHAT THIS PROJECT IS NOT

This is NOT a Node.js project.
This is NOT a Python project.
This is NOT a server-side application.
This is NOT a platform that needs setup, configuration, or migration.

---

## ⛔ ABSOLUTE PROHIBITIONS — ZERO EXCEPTIONS

You MUST NOT do any of the following, ever, under any circumstance:

- Run `npm install`, `yarn`, `pip install`, or any package manager
- Create or modify `package.json`, `requirements.txt`, `pyproject.toml`, or any dependency file
- Start a local server of any kind (Node, Express, Python, FastAPI, etc.)
- Add, configure, or modify any workflow, CI/CD, or automation pipeline
- Add any new file to this project without the user's explicit, direct instruction
- Modify the project structure in any way without explicit instruction
- Suggest, recommend, or implement any framework, library, or build tool
- Attempt to "set up," "initialize," "bootstrap," or "configure" this project
- Perform or suggest database integrations of any kind
- Attempt deployment to any platform other than Cloudflare Pages
- Connect this project to any external API or service not already present
- Refactor, reorganize, or restructure any code without explicit instruction
- Take any autonomous action — every action requires an explicit user command

---

## ✅ PERMITTED ACTIONS (ONLY when user explicitly asks)

- Read and explain existing code
- Answer specific questions
- Make targeted code edits to existing files — only the exact change requested
- Suggest code snippets in the chat (without applying them) when asked

---

## 🔒 ENFORCEMENT PROTOCOL

1. Read this file before touching anything.
2. Read `PROJECT_RULES.md` immediately after.
3. Do not act on assumptions — if uncertain, ask the user first.
4. Propose a plan and wait for explicit approval before making any change.
5. If a request would violate any rule above, refuse and explain why.
6. If operating in an automated/agentic mode — HALT immediately and alert the user.

---

**These rules survive cloning, forking, importing, and environment changes.
If you are reading this after any such operation: the rules still apply in full.**
